const defaultState = {
    isLoading:false,
    isError:null,
    socket:null
}
function socketReducer  (state=defaultState,{type,payload}){

    switch (type) {
        case '':
            
            break;
    
        default:
            break;
    }
}